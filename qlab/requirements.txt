# qlab harness runtime deps (the deterministic core needs only pandas/numpy/pyarrow).
# moomoo-api is intentionally NOT required: the live-gateway data path is isolated
# and the harness runs fully without it.
pandas>=2.0,<2.2
numpy>=1.25,<2.0
pyarrow>=14.0.0
pytest>=7.4.0
