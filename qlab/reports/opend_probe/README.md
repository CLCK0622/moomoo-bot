# Real OpenD SIMULATE evidence — order-path probe

First-hand metrics captured against the **live moomoo OpenD gateway + SIMULATE
US account** on the user's Mac mini (`127.0.0.1:11111`, OpenD/SDK 10.08.6808).
Generated by:

```bash
python -m qlab.opend_probe --symbol AAPL --n-orders 30 --out reports/opend_probe
```

Captured **2026-07-09, `market_us = AFTER_HOURS_END`** (US regular session
closed). Every order was a resting far-below-market limit (qty 1, ~50% below
last), cancelled immediately, with a final sweep + reconcile confirming the
account stayed flat — nothing could fill.

## What's real here (`opend_metrics.json`)

- **Connectivity**: connected 1st attempt, `trd_logined`/`qot_logined` = true,
  0 disconnects.
- **Account/quote perms**: SIMULATE account reachable (virtual 1,000,000);
  quote snapshot permitted (AAPL last 313.39).
- **Order submit→ack latency** (first-hand, `n=30`): **P50 ≈ 562 ms, P95 ≈ 678 ms,
  max ≈ 743 ms** (min ≈ 554 ms). The ~560 ms floor is the moomoo SDK/gateway
  order-path cost (incl. its order-frequency pacing), not our decision logic.
- **Cancel latency**: P50 ≈ 496 ms, P95 ≈ 647 ms, max ≈ 787 ms.
- **Accept / reject**: accept 100%, reject 0% (far-limit orders rest after hours;
  a meaningful reject-rate needs marketable orders in-session).
- **Position reconciliation**: engine-intent vs broker positions in sync, flat
  after the sweep.

`broker_events.jsonl` has the per-order `order_submitted` / `order_cancelled`
records with `submit_ts` / `ack_ts` / `latency_ms`, plus `position_reconcile`.

## Deferred (NOT fabricated — require the regular session)

`fill_rate`, `actual_slippage`, `partial_fill_behavior` need US regular hours
(09:30–16:00 ET). Per the brief (闭市别空跑实时指标) they are **not** measured
here; re-run during session to capture them. Latency / reject / reconcile above
remain valid regardless of session.

**No PnL/return claim.** SIMULATE virtual money is not a performance result.
